#include <iostream>
#include <iomanip>
using namespace std;
void OutputMatrix(double**, double*, int);
void CheckBefore(double**, double*, int, int);
void DoTheXnOne(double**, double*, int, int);
void Gauss(double**, double*, int);
void FindTheAnswer(double**, double*, double*, int);

